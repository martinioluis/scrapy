let connection;_8a4‍.w("../conf",[["default",["connection"],function(v){connection=v}]]);const { Router } = require('express');


const router = Router();

router.get('/', (req, res, next) => {
      connection.query('SELECT title FROM item', (error, results) => {
          if(error) {
              res.sendStatus(500);
          } else {
              res.json(results);
          }
      });
  });

module.exports = router;
